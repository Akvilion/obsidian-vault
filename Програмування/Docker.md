
| Варіант      | Працює з URL | Розпаковує `.tar.gz` | Коментар               |
| ------------ | ------------ | -------------------- | ---------------------- |
| `ADD URL`    | ✅            | ❌                    | Скачує як є            |
| `COPY URL`   | ❌            | ❌                    | Помилка                |
| `COPY локал` | ❌            | ❌                    | Просто копія           |
| `ADD локал`  | ❌            | ✅                    | Авто-розпаковка архіву |
## 🟦 Українською — висновки:

- **`ADD` із URL** працює як `wget` — просто завантажує.
    
- **`COPY` не працює з URL взагалі.**
    
- **`ADD` із `.tar.gz` локальним — автоматично розпаковує**.
    
- **`COPY` — тупо копіює, нічого не розпаковує.**


| Змінна | Доступна під час білду | Доступна у контейнері | Можна задавати через `--build-arg` |
| ------ | ---------------------- | --------------------- | ---------------------------------- |
| `ARG`  | ✅                      | ❌                     | ✅                                  |
| `ENV`  | ✅                      | ✅                     | ❌                                  |

один шар

FROM alpine:3.17

RUN apk add --no-cache \
    htop \
    postgresql-client \
    vim \
    git \
    jq=1.6-r2


5 шарів

FROM alpine:3.17

RUN apk add --no-cache htop
RUN apk add --no-cache postgresql-client
RUN apk add --no-cache vim
RUN apk add --no-cache git
RUN apk add --no-cache jq=1.6-r2


## 🧠 Пояснення для DevOps-воїна:

- Кожен `RUN`, `COPY`, `ADD` створює **новий шар**.
    
- Більше шарів → більше розміру → повільніший білд і push.
    
- Об'єднання команд в один `RUN` через `\` або `&&` → менше шарів, менше кешування, менше проблем.

|Docker інструкція|Створює шар?|Примітка|
|---|---|---|
|`FROM`|✅|Стартовий шар|
|`WORKDIR`|✅|Так, створює|
|`COPY`|✅|Завжди створює новий шар|
|`RUN`|✅|Кожен RUN = новий шар|
|`ENV`|✅|Так|
|`EXPOSE`|✅|Але шар 0B, просто мета-інфа|
|`ENTRYPOINT`|✅|Теж мета-інфа, 0B|



Dockerfile & Containerization Summary

Ось тобі, A12, гарне, чітке та системне **summary по темі "Робота з Dockerfile, іміджами, шарами та базовим реєстром"**, яке охоплює весь зміст пройдених Task 1–8 + відповіді на обговорення.

---

# 🐳 **Dockerfile & Containerization Summary**

## 🔧 Основи Dockerfile та побудова іміджів

### 📌 **Основні директиви Dockerfile:**

|Директива|Призначення|
|---|---|
|`FROM`|Визначає базовий імідж|
|`WORKDIR`|Встановлює робочу директорію у контейнері|
|`ENV`|Створює змінні середовища, які залишаються доступними під час виконання|
|`ARG`|Змінна доступна **лише на етапі білду**|
|`COPY`|Копіює файли із хоста до іміджу|
|`ADD`|Як `COPY`, але підтримує URL і авто-розпаковку `.tar.gz`|
|`RUN`|Виконує команду під час створення іміджу (створює новий шар)|
|`CMD`|Встановлює команду за замовчуванням (перезаписується при запуску контейнера)|
|`ENTRYPOINT`|Основна команда, яку завжди виконує контейнер (навіть із переданими аргументами)|
|`EXPOSE`|Документує, який порт відкритий у контейнері (але не проброшений)|
|`USER`|Задає користувача для подальших інструкцій|
|`LABEL`|Додає метаінформацію до іміджу (наприклад, опис)|

---

## ⚔️ **ENTRYPOINT vs CMD**

|Характеристика|ENTRYPOINT|CMD|
|---|---|---|
|Мета|Головна команда|Аргументи для ENTRYPOINT або fallback|
|Поведінка з аргументами|Не перезаписується|Перезаписується|
|Комбінування|ENTRYPOINT + CMD → CMD як аргументи||

📌 Якщо використовуються обидва:

```Dockerfile
ENTRYPOINT ["echo", "Hello"]
CMD ["World"]
```

→ виконується: `echo World`

---

## 📦 **Шари (Layers) та оптимізація**

- **Кожна інструкція `RUN`, `COPY`, `ADD` створює новий шар**
    
- `ENV`, `EXPOSE`, `WORKDIR` — теж створюють шар, але часто розмір 0B
    
- Менше шарів → менше кешування, менший розмір іміджу
    
- Об’єднуй `RUN` через `&&` або `\` щоб зменшити кількість шарів
    

✅ **Поганий приклад:**

```Dockerfile
RUN apk add htop
RUN apk add git
RUN apk add vim
```

✅ **Хороший приклад:**

```Dockerfile
RUN apk add --no-cache htop git vim
```

---

## 🪪 **ARG vs ENV**

|Порівняння|`ARG`|`ENV`|
|---|---|---|
|Видимість|Лише під час білду|Доступна у білді та рантаймі|
|Застосування|Значення для білду (`go build`)|Конфігурація в контейнері|
|Override|Через `--build-arg`|Через `-e` при запуску|

---

## 🌍 **EXPOSE — порт чи не порт?**

- `EXPOSE` не відкриває порт.
    
- Це **документація**, що контейнер **всередині слухає** на порту.
    
- Щоб реально працювало — треба `-p 8080:8080` або `-p 8080:80` при запуску.
    

---

## 📤 **Docker Registry: локальний реєстр**

1. Запуск:
    

```bash
docker run -d --name registry -p 5000:5000 --restart=always registry:2
```

2. Тег:
    

```bash
docker tag alpine:3.17 localhost:5000/my-alpine
```

3. Push:
    

```bash
docker push localhost:5000/my-alpine
```

4. Видалення локального образу:
    

```bash
docker rmi localhost:5000/my-alpine alpine:3.17
```

5. Pull з реєстру:
    

```bash
docker pull localhost:5000/my-alpine
```

---

## 🔥 **Multi-stage Docker Build з Go App**

```Dockerfile
# stage 1: build
FROM golang:1.19 AS builder
WORKDIR /app
COPY main.go .
ENV CGO_ENABLED=0
RUN go build -o hello main.go

# stage 2: minimal runtime
FROM scratch
COPY --from=builder /app/hello /hello
EXPOSE 8999
ENTRYPOINT ["/hello"]
```

📌 Final image має:

- 1 шар
    
- Нуль зайвих пакетів
    
- Мінімальний розмір
    

---

## ❌ **Міфи (ісправлення)**

> ❌ Кожна директива створює новий шар  
> ✅ **Ні.** Тільки ті, що змінюють файлову систему: `COPY`, `RUN`, `ADD`. Інші (як `EXPOSE`, `LABEL`, `ENV`) можуть створювати логічний шар, але без payload (0B).

---

## ✅ **Docker Best Practices Recap**

- Використовуй `ENTRYPOINT` для основної команди, `CMD` — для аргументів
    
- Мінімізуй кількість `RUN`
    
- Обирай `ADD` лише коли справді потрібно (архіви, URL)
    
- `EXPOSE` — тільки документація, використовуй `-p` для відкриття порту
    
- Робота з локальним реєстром — must-have навичка
    
- Multi-stage build — кращий спосіб робити продакшн-образи
    

---


| Тип монтування       | Приклад                                        | Де зберігаються дані                                           | Переваги                                                       | Недоліки                                                       | Живе після видалення контейнера? |
| -------------------- | ---------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------- |
| **Bind mount**       | `-v /host/path:/container/path`                | Прямо у вказаному каталозі на хості                            | Прямий доступ до файлів хоста; можна редагувати без контейнера | Можна ненароком змінити/пошкодити файли хоста                  | ✅                                |
| **Named volume**     | `-v volume_name:/container/path`               | `/var/lib/docker/volumes/volume_name/_data` (керується Docker) | Легко переносити між контейнерами, Docker керує місцем         | Не так просто дістатись напряму без `docker run` чи `sudo`     | ✅                                |
| **Anonymous volume** | `-v /container/path` (без імені та хост-шляху) | `/var/lib/docker/volumes/<random_id>/_data`                    | Простий для швидких тестів                                     | Легко загубити, бо ім’я випадкове; складно повторно підключити | ✅ (поки не видалиш)              |
| **tmpfs**            | `--mount type=tmpfs,destination=/path`         | В оперативній пам’яті                                          | Дуже швидкий доступ, без зносу SSD                             | Дані зникають після зупинки контейнера; обмеження по пам’яті   | ❌                                |

## **Ключові висновки**

- **Bind mount** — ідеально для девелопменту, коли треба бачити зміни файлів в реальному часі на хості.
    
- **Named volume** — краще для продакшн-даних, бо Docker сам менеджить їх іменування і місце зберігання.
    
- **Anonymous volume** — рідко використовують, хіба для тимчасових даних у одноразових контейнерах.
    
- **tmpfs** — підходить для кешів та швидких тимчасових обчислень, коли дані не треба зберігати.
    
- Можна **шарити томи між контейнерами** (`--volumes-from` або однаковий `-v volume_name:...`) — це основа sidecar-патерну.
    
- **Mountpoint** на хості для named/anonymous томів завжди у `/var/lib/docker/volumes/.../_data`.
    
- **Bind mounts видно напряму** у файловій системі хоста, **volumes** — ні (без root-доступу).